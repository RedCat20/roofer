

export interface IGridConfig {
    width: number,
    height: number,
    startCoords: {
        x: number,
        y: number,
    },
    cellSize: number,
    gridHorizontalNumbers: number,
    gridVerticalNumbers: number,
}